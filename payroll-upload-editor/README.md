v8 요약

- 작업 파일: `payroll-upload-editor.delete-marked-empty-columns-v8.html`
- 기준 파일: `v7` 전체 미리보기 버전

이번 수정

- Step 5의 `값없는 열 일괄삭제`를 `값없음 표시 열 삭제`로 변경
- Step 5에서는 화면에 실제로 `값없음/0` 경고가 붙은 헤더만 읽어서 삭제
- 확인창 개수도 화면에 표시된 경고 열 개수와 같도록 수정

원인 분석

- 이전 버전은 내부 데이터의 숨은 뒤쪽 열까지 같이 세는 경우가 있어 실제 보이는 열보다 삭제 개수가 크게 보일 수 있었음
- 이번 버전은 Step 5 렌더링 결과인 `th.empty-col-warning`만 기준으로 삭제함

